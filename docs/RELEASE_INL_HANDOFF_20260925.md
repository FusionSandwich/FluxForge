# FluxForge release and INL validation handoff — 25 September 2026

This is a continuation receipt, not a release certificate or an accepted INL result. It is intended for a future ChatGPT Pro chat with access to this GitHub repository. Check live branch heads and test results before acting; dates and counts below describe the evidence available at this handoff.

## Repository state and reading order

- The intended integration branch is `optimization-workflows` (`c297542` when checked). HPGe PR [#183](https://github.com/FusionSandwich/FluxForge/pull/183) is open against it from `JS/hpge-03-calibration-efficiency`. This handoff and the viewport fix are on the PR branch.
- `recovery-validation` (`394efdc` when checked) carries the newer scientific recovery implementation and release/INL receipts. At the check, it was 11 commits ahead of `optimization-workflows`, while the HPGe PR branch was four commits ahead; the two branches had diverged. The newest commit date alone does not identify a complete release baseline. Recompare before integration.
- Read, in order, `docs/RELEASE_RECOVERY_STATE.md`, `docs/INL_BACKGROUND_IMPLEMENTATION_RECEIPT.md`, `docs/BACKGROUND_COVARIANCE_CONTRACT.md`, `docs/RECOVERY_TODO_20260916.md`, and `docs/FEATURE_VALIDATION_MATRIX.md` **on `recovery-validation`**. The September 21 `docs/CHATGPT_PRO_CONTINUATION_20260921.md` supersedes older background-grid diagnoses. Read this branch's `docs/BRANCH03_HPGE_EVIDENCE.md` and `docs/HPGE_EFFICIENCY_CALIBRATION.md` for the separate HPGe work.
- Older roadmap and RAFM artifacts are not current acceptance receipts. In particular, `examples/RAFM_irradiation/results/validation_summary.md` says “Overall pass: yes,” but `examples/RAFM_irradiation/count_parity_llm_handoff.md` says honest flux-wire count parity remains unfinished and failing. Reconcile this before quoting a validation result.

## Completed work and evidence on the HPGe PR branch

- HPGe efficiency-point CSV input, three fit families, diagnostics, conditional covariance, spectrum-specific detector profiles, session persistence, undo, and downstream activity uncertainty are implemented. The branch evidence document records what was tested and the scientific limitations, including unvalidated geometry transfer and certificate import.
- A stale Qt canvas viewport was reproduced locally: zoom to 0–1, clear, then load a 1,000-channel spectrum; the trace buffer covered the spectrum while the view remained at 0–1. `PyQtGraphSpectrumCanvas.set_traces` now autoranges the first nonempty trace after clear, and `clear` cancels a pending old viewport commit. `tests/test_pyqtgraph_viewport.py` has a regression for the observed case. An independent Sol review then found a counterexample: the workspace's cached unchanged viewport could skip reapplying a saved narrow range after temporary clear/reload. `modern_shell_center.py` now forces canonical viewport application on the first nonempty load, with a regression in `tests/test_bgamma_canvas_integration_qt.py`.
- Four bounded Luna assignments at the pre-fix PR head passed: HPGe dialog 5, profile persistence 8, viewport/workspace 3, and GUI catalog/parity/goal contracts 31. The Luna viewport review identified the untested clear/reload path; the Sol orchestrator reproduced and fixed it. These are software checks, not scientific acceptance.
- After the first viewport change, the focused viewport/workspace run passed 4 tests; the affected `test_bgamma_canvas_integration_qt.py` plus `test_analysis_workspace_qt.py` run passed 39 tests in offscreen Qt on Windows. After the Sol review fix, four targeted viewport tests passed, followed by 33 passing tests in `test_bgamma_canvas_integration_qt.py`, `test_modern_gui_shell.py` and `test_pyqtgraph_viewport.py` (109.06 seconds). Black and `git diff --check` passed on the final code. Flake8 reports ten pre-existing issues in `modern_shell_center.py`, identical to the committed HEAD baseline; it reports no new issue. The GUI contract test rewrote only the timestamp in `tests/goal_validation_report.json`; that generated change was restored.
- Prior HPGe branch evidence includes native Windows focused tests and 200% scaling screenshots. The full repository suite, visible-window endurance, Linux Qt/Wayland parity, installer and accessibility gates remain open. Do not extrapolate an offscreen Qt pass to those gates.

## Attempts that did not establish acceptance

- A previous optional repository-wide `pytest tests -q` run was stopped near 4%; it produced no full-suite verdict. A previous broad non-GUI core run was interrupted after roughly three minutes without a summary, so its pass/fail count is unknown. A new bounded core regression run completed during this handoff; its exact scope and result are below.
- Earlier HPGe integration testing exposed a Windows Qt heap-corruption exit during pytest teardown despite passing assertions. Focused teardown fixes and later separate-file runs passed, but broad teardown behavior is not fully qualified. The recovery branch's older native GUI run also ended abnormally; subsequent bounded runs passed without proving the original cause.
- In `recovery-validation`, conservative background-grid alignment and per-spectrum covariance have numerical oracles, but the original raw ten-bin INL regression still fails in the legacy counting-to-reaction pathway. Historical reference overrides and marginal-error substitutions must not be used to force a pass. Two RAFM sample axes lack full measured-background coverage, and two activity-parity fixtures lack required net-count uncertainty.
- The latest recovery commit adds viewport recovery code and tests, but its earlier receipt still requests persistent-window visual verification. Keep source implementation, automated checks and manual acceptance distinct.

## Work remaining, in order

1. Reconcile HPGe PR #183 with `recovery-validation` on the intended integration branch. Both changed core workspace and GUI canvas/analysis files. Review each overlapping change, preserve the covariance and viewport contracts, and rerun affected tests. Do not infer that either branch already contains the other.
2. Assign **Sol**, not Luna, to INL scientific decisions. Build a provenance-bound, covariance-aware raw counting-to-reaction reduction; propagate cross-peak and shared-background cross-sample covariance into reaction rates and GLS. Resolve the real-data ten-bin regression without reference overrides, fabricated uncertainty, calibration replacement, zero padding or disabled subtraction. Record conditional ROI-selection assumptions.
3. Bind original spectrum identities, detector/geometry/efficiency calibration, acquisition and irradiation timing, background suitability, response and nuclear-data provenance. Validate applicable unfolding methods and external dosimetry against independent expectations, with residuals and full uncertainty semantics. Passing synthetic solver tests or finite outputs do not validate INL measurements.
4. Reconcile every row of the feature matrix and the release checklist with current code, tests, skips, installed-package checks and platform evidence. Finish visible native GUI, Linux, installer, accessibility, DPI/multi-monitor and endurance gates as applicable. Update stale roadmap, parity and RAFM summary wording only when supported by evidence.
5. Keep G1–G4 incomplete until their separate receipts satisfy every requirement. There is no first official release or accepted experimental INL result in this handoff.

## Bounded continuation command

Use a fresh checkout of the current branch heads. For the HPGe viewport change, run `python -m pytest -q tests/test_pyqtgraph_viewport.py tests/test_bgamma_canvas_integration_qt.py tests/test_analysis_workspace_qt.py` in an existing environment with Qt available. Use separate installed-package and native-platform receipts for release qualification. Do not dispatch remote CI or perform remote scientific jobs on the authority of this handoff; obtain the user's current instructions first.

## Local core run at handoff

Command: `python -m pytest -q tests/test_hpge_efficiency_models.py tests/test_hpge_profile_persistence.py tests/test_detector_calibration.py tests/test_activity_review.py tests/test_workspace_document_v2.py tests/test_session_v2_migration.py tests/test_atomic_session_save.py tests/test_flux_wire_analysis.py` using the existing Windows QA interpreter and `MPLBACKEND=Agg`. Exit 0: **88 passed, 10 skipped in 48.88 seconds**. The skipped cases were not individually classified in this bounded run; the full suite remains unrun.
