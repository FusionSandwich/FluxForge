# Recovery state — 16 September 2026

## Strict goal status rule

G1–G4 are independent completion gates. No goal may be marked complete until all of its required acceptance evidence is present; partial results, software-only checks, documented blockers, or completion of another goal do not satisfy the missing gate. The overall handoff is complete only when all four goals are complete. The current `partial` and `blocked` labels below are therefore deliberate and must remain until their listed receipts exist.

- Selected source: `C:\Users\Josh\projects\FluxForge-validation`, branch `recovery-validation`, base `c297542da781291c3d1461af181b79349bda3087`.
- Selection: current remote tip confirmed with `ls-remote`; original checkout is an ancestor, 0 unique / 28 behind. Workspace-state and production-shell development commits have equivalent integrated patches; direct manipulation is integrated by ancestry. No upstream merge or push performed.
- Original checkout: `C:\Users\Josh\projects\FluxForge`, `39bfc708a901f3f9c06ddb4105d3634566ad14f9`; modified package metadata and untracked build/environment/install notes remain untouched.
- Preservation: `C:\Users\Josh\projects\FluxForge-recovery-20260916`; verified pre-fetch bundle, binary staged/unstaged patches, refs/reflog/status, and verified 1,591-file working-content archive. The generated `pre-fetch.bundle` and `working-content.zip` were relocated to `D:\FluxForge-recovery-20260916\archives` with unchanged hashes to recover disk space; the original recovery directory contains the relocation receipt. Build, dist and original virtual environments remain in place. Original sources and prior result files were not removed.
- Other copies: `C:\Users\Josh\projects\UWNR\FluxForge`, clean `main` at `52759a9`; Debian `/home/smandych/projects/FluxForge`, clean `core_CLI` at `1429d39`. The latter is a bulk snapshot of the older development source with the GUI omitted; its six non-GUI source differences from `39bfc70` are symbol/comment renames, not numerical improvements. Preserve it without replacing the selected Qt implementation.
- Search coverage: projects, Documents, Downloads, Desktop, relevant top-level D: roots, targeted university OneDrive project directories, and Debian `/home/smandych/projects`. The alternate-user `_work` path and historical `/filespace` path are absent. Other cloud-only/unmounted locations are not assumed searched.
- Required current handoffs: all three found and read in Downloads; existing local review reused.
- Installed identity: shared `projects\.venv` resolves the older original source through an editable installation; original checkout `venv` cannot import the package. The separate recovery `windows-env` now imports the non-editable package solely from its own `Lib\site-packages`; native Qt startup outside the checkout passed.
- Launcher: `fluxforge-gui` selects modern Qt; the installed wheel excludes the legacy package/entrypoint and the frozen launch script selects Qt. The local verified installation is separate from the unchanged older environments.
- Study worktree: `C:\Users\Josh\projects\rafm-analysis`, unprefixed `measurement-validation`, base `2bfbd81798349a0a5831d0f3ac8f698192034379`. Current study contracts are retained; the divergent older analysis branch is not blindly merged.
- Additional local-only work: Debian study `reduction` at `49eb40e` has six dirty source files and one untracked loader. Verified bundles, binary diffs and a seven-file source archive are under recovery `wsl`; unpublished `alara_output` remains untouched in place. Windows study original is clean at `9b043515`.
- INL sources: Windows and Debian study spectra/reports, existing FluxForge examples, Downloads converted corpus and university OneDrive project leads. Derived audit confirms 32 count identities and 29 clock conflicts; actual source availability is being reconciled by hashes.
- C — correctness: partial. GLS, activity timing/uncertainty, CLI count-area and detector attenuation-unit defects are fixed with independent numerical tests. Physical calibration, complete covariance and external dosimetry qualification remain open; no accepted experimental result is claimed. Reader/calibration, signed background, ROI estimator variance and shared-width fitting fixes are integrated. Unsupported background-grid transforms are now rejected; eight historical RAFM/flux-wire checks expose this workflow blocker.
- D — data: partial, experimental acceptance blocked. The v2 run parses 47 distinct accessible ASC byte identities from 129 paths and reduces all 2,768 detected candidates with provisional fixed-width sidebands. This covers 26 exact audited ASC identities; four indexed ASC hashes are missing and two audit rows have no ASC index. Twenty-one additional hashes remain physically unassociated. All 527 checked inputs are unchanged. Missing metadata and unresolved overlap remain explicit.
- W — Windows: partial; refreshed installed native checks now pass (see follow-up below). Historical baseline: Wheel `81c41a1d107d8dde8bf56d7c63713f82750d8434644d51e9767bc4ab4832faf2` has 271 members, clean installed identity/dependencies and verified packaged background resources. The integrated candidate passes 99 core/reader checks and one optional HTML check; the final affected N42 slice passes 18. Eight historical grid-mismatch workflows remain blocked. A refreshed native batch, isolated test and minimal PySide6 probe stall in `QApplication([])` before application initialization. Earlier native passes remain historical; interactive retry and broader manual qualification remain open.
- A — archive/cleanup: scoped isolation verified. Original files remain intact, restoration passes, normal packaging excludes the old interface and one maintained Qt launcher is documented. Complete historical functional parity is not claimed.
- Latest source tests: 70 inverse-contract tests pass, 10 external-reference skips; 100 covariance/solver checks pass; 107 CLI/artifact/contract checks pass. Installed baseline: 72 core checks pass with 9 optional-schema skips, 5 session checks pass, and 9 production-shell checks pass. Overlapping slices must not be added as a distinct-test total.
- Legacy recovery: 16-member exact-source archive restored with matching hashes; automated native Tk instantiation/close passes. See `LEGACY_GUI_RECOVERY.md`; this is not manual functional parity.
- Active blockers: physical calibration/time bindings, missing original spectrum identities, covariance-complete experimental reductions, covariance-preserving background-grid alignment and external dosimetry references. The original editable-environment mismatch is resolved by the separate verified install.
- Existing trackers: `ROADMAP_EXECUTION_STATUS.md`, `GUI_TEST_COVERAGE_LEDGER.md`, and study-owned measurement/result contracts.
- Deferred: SLOWPOKE qualification, full Linux parity, public release, unrelated transport campaigns.
- Preservation check: all 1,591 manifest files in the original checkout remain byte-identical; the latest receipt is `D:\FluxForge-feature-validation-20260916\integration-20260917\original-source-final-verification.json`. The versioned follow-up source/study snapshot is `D:\FluxForge-recovery-20260916\final-local-changes-v2-20260917`; the earlier checkpoint remains intact.
- Current task list: [recovery checklist](RECOVERY_TODO_20260916.md). The supplied email-search narrative remains an unverified lead; the local PDF, CSV and ZIP were independently inspected.

- Background follow-up: automatic normalization and explicit energy-axis preservation are corrected and source verified (112 focused checks) in `D:\FluxForge-background-followup-20260917\source`; integration and installed refresh are complete; current receipts are under `D:\FluxForge-integrated-followup-20260917`. See [follow-up receipt](BACKGROUND_FOLLOWUP_20260917.md). The eight grid-mismatch workflows remain blocked pending covariance-aware rebinning.

## Integrated follow-up verification - 18 September 2026

The validation checkout now resolves through the authoritative D: offload copy. Its Git worktree pointer was repaired without changing the branch or commit. Background normalization and explicit energy-axis fixes are integrated; finite signed spectra with supplied uncertainties now persist through FFS sessions. Conservative histogram rebinning with full sparse covariance is implemented and tested as a standalone primitive. It is not yet wired into the existing background workflows.

Wheel SHA-256: `fc179bd1b3b6fcfec0ce33bee9ea464545786b84725e8d8e4ef94c3278e1c023` (272 members). Installed in the separate recovery environment. Tests ran outside the source checkout and verified imports from site-packages.

- Installed numerical/reader/session/schema checks: 191 passed, one POSIX-only skip.
- Installed native Windows session/recovery checks: 11 passed; optional HTML test skipped in that environment.
- The same HTML check with the isolated optional dependency: one passed.
- Native source workflow run: 45 passed, one optional HTML skip. These overlap the installed checks and are not additional distinct coverage.
- Original-source preservation: all 1591 archived-manifest files match the authoritative offload copy.

Receipts: `D:\FluxForge-integrated-followup-20260917\installed-core.txt`, `installed-native.txt`, `installed-html.txt`, `wheel-identity.json`, and `original-preservation.json`.

Native Qt initialization and the selected session workflows are no longer blocked in this run. Full DPI, multi-monitor and endurance qualification remains open. Eight historical mismatched-grid workflows remain blocked by the existing guard until covariance is propagated through spectrum/session storage, ROI estimators and peak fitting. Physical calibration/time qualification and external dosimetry parity remain separate open gates.
